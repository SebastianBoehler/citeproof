# Example Draft

Method X outperforms PPO on sparse-reward robotics tasks \cite{smith2024methodx}.

Adaptive replay improves sample efficiency in sparse-reward manipulation tasks \cite{jones2023adaptive}.

The Method X ablation removes exploration entirely \cite{smith2024methodx}.

MissingSource improves all robotics tasks \cite{missing2026source}.
